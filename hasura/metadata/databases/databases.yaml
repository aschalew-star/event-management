- name: event_db
  kind: postgres
  configuration:
    connection_info:
      database_url: postgres://postgres:postgres@postgres:5432/event_db
      isolation_level: read-committed
      use_prepared_statements: false
  tables: "!include event_db/tables/tables.yaml"
